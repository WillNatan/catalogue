{{ form_start(form) }}
    {{ form_widget(form) }}

<a href="{{ path('Utilisateurs') }}" class="btn btn-default mr-1"><i class="fa fa-arrow-left"></i></a>
    <button class="btn btn-success mr-1">{{ button_label|default('Enregistrer') }}</button>



{{ form_end(form) }}