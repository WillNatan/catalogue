{{ form_start(form) }}
    {{ form_widget(form) }}
<div class="d-flex mt-3">
    <a href="{{ path('Utilisateurs') }}" class="btn gradient-btn-primary mr-1"><i class="fa fa-arrow-left"></i></a>
    <button class="btn gradient-btn mr-1">{{ button_label|default('Enregistrer') }}</button>



