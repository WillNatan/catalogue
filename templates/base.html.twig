<!DOCTYPE html>
{% if app.request.attributes.get('_route') == 'app_login' %}
<html class="h-100 w-100">
{% else %}
<html>
{% endif %}
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <link rel="stylesheet" href="{{ asset('dashboard/css/sb-admin-2.min.css') }}">
        <link rel="stylesheet" href="{{ asset('dashboard/css/custom.css') }}">
        {% block stylesheets %}{% endblock %}
    </head>
    {% if app.request.attributes.get('_route') == 'app_login' %}
    <body class="h-100 w-100" style="background-image: url('{{ asset('dashboard/img/'~ random(['login1.jpg', 'login2.jpg', 'login3.jpg', 'login4.jpg'])) }}')">
    <div class="container h-100 w-100 d-flex justify-content-center align-items-center">
    {% else %}
        <body>
        <div class="container">
    {% endif %}

            {% block body %}{% endblock %}
        </div>
        <script src="{{ asset('dashboard/vendor/jquery/jquery.min.js') }}"></script>
        <script src="{{ asset('dashboard/vendor/bootstrap/js/bootstrap.min.js') }}"></script>
        <script src="{{ asset('dashboard/js/sb-admin-2.min.js') }}"></script>
        {% block javascripts %}{% endblock %}
    </body>
</html>
